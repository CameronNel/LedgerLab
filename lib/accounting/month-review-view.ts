/** Escaped, shared review presentation for the local workstation, React and portable export. */
import {htmlEscape as e} from './exports';
import {money} from './money';
import type {PracticeCompany,PracticeState} from './types';
import type {MonthAssessment,MonthCheck,ReviewDifference} from './month-review-types';

export const reviewStyles=`
.mr-panel{scroll-margin-top:70px;font:14px/1.55 system-ui,sans-serif;color:#17312f;background:#fff;border:1px solid #cddbd7;border-radius:12px;overflow:hidden;margin:18px 0;min-width:0}.mr-panel *{box-sizing:border-box}.mr-head{padding:24px 26px;background:#edf6f2;border-bottom:1px solid #d4e4dd}.mr-eyebrow{font-size:11px;font-weight:750;letter-spacing:.12em;color:#3b6659}.mr-head h2{font-size:26px;line-height:1.2;margin:8px 0 10px;color:#163b31}.mr-head p{max-width:850px;margin:6px 0}.mr-actions{display:flex;gap:9px;flex-wrap:wrap;margin-top:18px}.mr-panel button{border:1px solid #bdcec7;border-radius:6px;padding:8px 13px;color:#1a4236;background:#fff;cursor:pointer;font:inherit;font-weight:600}.mr-panel button:hover{background:#eef7f2}.mr-panel button:disabled{opacity:.5;cursor:default}.mr-panel .mr-primary{background:#246549;color:#fff;border-color:#246549}.mr-panel .mr-primary:hover{background:#194d38}.mr-stats{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:14px;margin-top:20px}.mr-stat{background:#fff;border:1px solid #d6e4dd;border-radius:7px;padding:12px}.mr-stat strong{display:block;font-size:24px;line-height:1.15}.mr-stat span{display:block;font-size:12px;color:#486258;margin-top:6px}.mr-body{padding:18px 26px 24px}.mr-notice{border-left:4px solid #c89234;background:#fff6e6;padding:11px 14px;margin-bottom:14px;font-size:13px}.mr-notice.mr-good{border-color:#318765;background:#edf8f1}.mr-history{margin:14px 0;color:#486158;font-size:12px}.mr-history table{border-collapse:collapse;width:100%;margin-top:10px}.mr-history th,.mr-history td{padding:7px;text-align:left;border-bottom:1px solid #dce6e1}.mr-controls{display:flex;align-items:center;gap:12px;flex-wrap:wrap;margin:16px 0}.mr-controls label{display:flex;align-items:center;gap:7px;font-size:13px}.mr-controls input[type=checkbox]{width:16px;height:16px}.mr-area{border:1px solid #d7e2dc;border-radius:7px;margin:10px 0;overflow:hidden}.mr-area>summary{list-style:none;background:#f5f8f6;padding:13px 16px;cursor:pointer;font-weight:700;display:flex;justify-content:space-between;align-items:center;gap:12px}.mr-area>summary::before{content:'▸';color:#44715e}.mr-area[open]>summary::before{content:'▾'}.mr-area-title{flex:1}.mr-area-count{font-weight:500;font-size:12px;color:#4e665d}.mr-area-content{padding:2px 13px 10px}.mr-check{margin:8px 0;border:1px solid #e0e7e3;border-radius:6px;background:white}.mr-check>summary{display:flex;align-items:flex-start;gap:10px;padding:10px 12px;cursor:pointer;list-style:none;font-size:13px}.mr-check-title{flex:1;overflow-wrap:anywhere}.mr-badge{font-size:10px;line-height:1.5;letter-spacing:.03em;font-weight:750;padding:3px 7px;border-radius:4px;white-space:nowrap;background:#e9f4ed;color:#286443}.mr-check[data-status=missing] .mr-badge{background:#fff0d6;color:#8b5712}.mr-check[data-status=incorrect] .mr-badge{background:#fdebea;color:#a43834}.mr-check[data-status=warning] .mr-badge{background:#fff0d6;color:#8b5712}.mr-check[data-status=manual] .mr-badge{background:#eaf0fa;color:#405c8b}.mr-check-content{padding:0 14px 14px}.mr-check-content p{margin:9px 0}.mr-check-content .mr-next{background:#f5f8f6;padding:10px 12px;border-radius:4px}.mr-table{overflow:auto;margin:12px 0}.mr-table table{border-collapse:collapse;width:100%;font-size:12px;min-width:600px}.mr-table th,.mr-table td{border-bottom:1px solid #e0e6e2;text-align:right;padding:8px;vertical-align:top}.mr-table th:first-child,.mr-table td:first-child{text-align:left;min-width:175px}.mr-table th{background:#f0f5f2;color:#3c584d}.mr-table small{display:block;color:#597064}.mr-links{display:flex;gap:7px;flex-wrap:wrap}.mr-links button{font-size:12px;padding:5px 9px}.mr-panel[data-show-passed=false] .mr-check[data-status=pass]{display:none}.mr-panel [hidden]{display:none!important}.mr-area-note{font-size:12px;color:#5a7265;margin:12px 3px 3px}.mr-end{font-size:12px;color:#526a5e;border-top:1px solid #dbe6df;margin-top:20px;padding-top:14px}.mr-sign{font-variant-numeric:tabular-nums}.mr-draft-status{margin-top:10px;font-size:12px;font-weight:600}.mr-statusline{font-size:12px;color:#536b60}.mr-panel [data-review-search]{padding:7px 10px;min-width:190px;border:1px solid #bdcec7;border-radius:5px;background:#fff;color:#203c31}.mr-focus{outline:2px solid #50896a;outline-offset:2px}@media(max-width:800px){.mr-stats{grid-template-columns:repeat(2,minmax(0,1fr))}.mr-head,.mr-body{padding:16px}.mr-head h2{font-size:23px}}@media print{.mr-panel{border:0}.mr-actions,.mr-controls,.mr-links{display:none}.mr-area,.mr-check{break-inside:avoid}.mr-head{background:#fff}.mr-panel .mr-check[data-status=pass]{display:none}.mr-table table{min-width:0}}
`;
export function feedbackVisible(state:PracticeState,r:MonthAssessment):boolean {
  return state.mode==='guided'||!!state.monthReviews?.some(v=>v.month===r.month&&v.fingerprint===r.fingerprint&&v.feedbackRevealed);
}
const badge=(s:string)=>({pass:'PASS',missing:'MISSING',incorrect:'INCORRECT',warning:'REVIEW',manual:'HUMAN REVIEW'}[s]??s);
function differenceRow(d:ReviewDifference,answers:boolean){
  const detail=[d.date,d.contact?'Contact '+d.contact:'',d.cashClass?'Cash flow: '+d.cashClass:''].filter(Boolean).join(' · ');
  const correction=d.actual===null?null:d.expected-d.actual;
  return `<tr><td>${e(d.label)}${detail?`<small>${e(detail)}</small>`:''}</td><td>${d.actual===null?'Not supplied':money(d.actual)}</td><td>${answers?money(d.expected):'Hidden until submission'}</td><td>${answers?(d.difference===null?'Not supplied':money(d.difference)):'Hidden'}</td>${d.account?`<td>${answers&&correction!==null?`${correction>0?'Dr':correction<0?'Cr':'None'} ${money(Math.abs(correction))}`:'Hidden'}</td>`:''}</tr>`;
}
function checkHTML(c:MonthCheck,answers:boolean,exporting:boolean){
  const posting=c.differences.some(d=>d.account);
  return `<details class="mr-check" data-status="${c.status}" data-check-id="${e(c.id)}" ${exporting?'open':''}><summary><span class="mr-badge">${badge(c.status)}</span><span class="mr-check-title">${e(c.title)}</span><span aria-hidden="true">+</span></summary><div class="mr-check-content"><p>${e(c.explanation)}</p>${c.differences.length?`<div class="mr-table"><table><thead><tr><th>Check / account</th><th>Your saved amount</th><th>Case answer</th><th>Yours less answer</th>${posting?'<th>Net correction</th>':''}</tr></thead><tbody>${c.differences.map(d=>differenceRow(d,answers)).join('')}</tbody></table></div>${posting?'<p class="mr-statusline">AUD; signed net postings: positive = debit, negative = credit. These are differences grouped by date, source, account, contact and cash-flow class, not a prebalanced journal to copy.</p>':''}`:''}<p class="mr-next"><strong>Next action:</strong> ${e(c.action)}</p>${exporting?c.sourceIds.length?`<p>Evidence: ${c.sourceIds.map(e).join(', ')}</p>`:'':`<div class="mr-links"><button type="button" data-review-action="work" data-view="${e(c.view)}">Open work area</button>${c.sourceIds.map(id=>`<button type="button" data-review-action="source" data-id="${e(id)}">${e(id)}</button>`).join('')}</div>`}</div></details>`;
}
export function monthReviewHTML(company:PracticeCompany,state:PracticeState,r:MonthAssessment,options:{exporting?:boolean;at?:string}={}):string {
  const exporting=!!options.exporting,answers=feedbackVisible(state,r),history=(state.monthReviews??[]).filter(v=>v.month===r.month),last=history.at(-1),previous=history.at(-2),unchanged=last?.fingerprint===r.fingerprint;
  const missing=r.checks.filter(c=>c.blocking&&c.status==='missing').length,incorrect=r.checks.filter(c=>c.blocking&&c.status==='incorrect').length,areas=[...new Set(r.checks.map(c=>c.area))];
  const resolved=previous&&last?previous.issueIds.filter(id=>!last.issueIds.includes(id)).length:0,added=previous&&last?last.issueIds.filter(id=>!previous.issueIds.includes(id)).length:0;
  const closed=!!state.career?.closed[r.month];
  let html=`<section class="mr-panel" data-show-passed="false" aria-label="Month review"><div class="mr-head"><div class="mr-eyebrow">MONTH REVIEW / ${e(r.month)} / ${e(company.currency)}</div><h2>${r.ready?'Automated checks passed. Review the judgement.':'Find what is missing. Understand what is wrong.'}</h2><p>Compare saved work with the independent case answers, then correct and recheck. <strong>Submitting a review does not post, close or advance the month.</strong></p>${exporting?'':`<div class="mr-actions"><button type="button" data-review-action="check">Check my work</button><button type="button" class="mr-primary" data-review-action="submit" ${closed||unchanged?'disabled':''}>${unchanged?'Review submitted':'Submit month for review'}</button><button type="button" data-review-action="export">Export feedback</button></div>`}<div class="mr-stats"><div class="mr-stat"><strong>${r.passed} / ${r.total}</strong><span>Objective / completion checks pass</span></div><div class="mr-stat"><strong>${missing}</strong><span>Missing or not submitted</span></div><div class="mr-stat"><strong>${incorrect}</strong><span>Incorrect checks</span></div><div class="mr-stat"><strong>${r.manual}</strong><span>Categories needing human review</span></div></div></div><div class="mr-body"><div class="mr-notice ${r.ready?'mr-good':''}">${r.ready?'<strong>Ready for the case’s automated close gates.</strong> This is not a certification of professional competence or complete statutory AFS.':`<strong>${r.blockers} blocking checks remain.</strong> A source error may also affect several balances and reports, so this is a checklist count, not a count of unique mistakes.`}${r.warnings?` ${r.warnings} supporting-work warning(s) also need review.`:''}</div><p class="mr-statusline">Checks cover the active takeover month, its closing balances and any earlier unresolved source exceptions. Future-month actuals are not assessed or revealed. All amounts are AUD.</p><div class="mr-notice"><strong>Save first.</strong> Only saved ledger entries, bank matches, deliverable submissions and forecasts are assessed. Unsaved forms and workbook cells are excluded. Freeform Excel workbooks are not automatically marked; transfer the requested results into the formal deliverables.</div><p class="mr-draft-status">${!last?'No saved review attempt for this month.':unchanged?`Latest submitted review matches this saved work (${e(last.at)}).`:'STALE REVIEW: saved work has changed since the last submission. Check and submit again.'} ${answers?'Case answers are visible for this assessment.':'On-the-job mode: diagnoses are visible; numerical answers are hidden until you submit this saved attempt.'}</p>${last&&previous&&unchanged?`<p class="mr-statusline">Since the previous saved attempt: ${resolved} exceptions resolved; ${added} new exceptions.</p>`:''}`;
  if(!exporting)html+=`<div class="mr-controls"><label><input type="checkbox" data-review-passed>Show passed checks</label><input type="search" data-review-search aria-label="Search month feedback" placeholder="Search source, account or task"></div>`;
  for(const area of areas){const rows=r.checks.filter(c=>c.area===area),passing=rows.filter(c=>c.status==='pass').length,open=exporting;
    html+=`<details class="mr-area" ${open?'open':''}><summary><span class="mr-area-title">${e(area)}</span><span class="mr-area-count">${area==='Requires professional review'?`${rows.length} not auto-graded`:`${passing} / ${rows.length} pass`}</span></summary><div class="mr-area-content">${rows.filter(c=>!exporting||c.status!=='pass').map(c=>checkHTML(c,answers,exporting)).join('')}${passing===rows.length?'<p class="mr-area-note">No exceptions in this area. Enable “Show passed checks” to inspect the checks.</p>':''}</div></details>`;
  }
  html+=`<details class="mr-history"><summary>Saved review history · ${history.length} retained for this month</summary><p>Latest 24 attempts across the case are retained as summary receipts. Export detailed feedback when submitting for a full contemporaneous report. Receipts are learning history, not tamper-proof exam results.</p>${history.length?`<table><thead><tr><th>Submitted</th><th>Checks passed</th><th>Blockers</th><th>Feedback</th></tr></thead><tbody>${[...history].reverse().map(v=>`<tr><td>${e(v.at)}</td><td>${v.passed} / ${v.total}</td><td>${v.blockers}</td><td>${v.feedbackRevealed?'Answers shown':'Hidden'}</td></tr>`).join('')}</tbody></table>`:'<p>No attempt submitted yet.</p>'}</details><p class="mr-end">Assessment is specific to this fictional single-entity AUD 2025 scenario. Source postings are compared exactly in cents, date, coding, contact, evidence link and cash-flow class; split journals and net reversals may agree. An alternative real-world treatment can require professional discussion rather than matching this case key. Written judgement, workbook completeness, forecast realism and statutory sufficiency are not independently graded. ${options.at?`Generated ${e(options.at)}.`:''}</p></div></section>`;
  return html;
}
export function monthReviewDocument(company:PracticeCompany,state:PracticeState,r:MonthAssessment,at=new Date().toISOString()):string {
  return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>LedgerLab ${e(r.month)} month review</title><style>body{margin:24px auto;max-width:1150px;padding:0 16px;background:#f7f9f7}${reviewStyles}</style></head><body>${monthReviewHTML(company,state,r,{exporting:true,at})}</body></html>`;
}
/** Search/filter existing escaped DOM without recreating draft forms or losing focus. */
export function filterReview(root:HTMLElement,query:string,showPassed:boolean):void {
  root.dataset.showPassed=String(showPassed);
  for(const row of root.querySelectorAll<HTMLElement>('.mr-check'))row.hidden=!!query&&!row.textContent?.toLowerCase().includes(query.trim().toLowerCase());
}
